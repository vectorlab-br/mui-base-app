import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette:{
    primary:{
      main: "#1D4289",
      light: "skyblue"
    },
    secondary:{
      main: "#5F249F"
    },
    otherColor: {
      main: "#999"
    }
  }
})